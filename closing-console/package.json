{
  "name": "closing-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --persist-to .wrangler/state",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply dcc --local",
    "migrate:remote": "wrangler d1 migrations apply dcc --remote"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
